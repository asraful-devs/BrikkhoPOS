# How to Start React with Vite

go to the project directory and run:

```bash
npm create vite@latest
```

Then, follow the prompts to set up your React project with Vite.

then setup shadcn & tailwindcss follow the official documentation:

dark & light mode setup:

main.tsx file: theameProvider wraper

create provider folder and theame-provider.tsx file
also create mode-toggle.tsx file for toggle button

then react-router-dom setup:

```bash
npm install react-router-dom
```

select data mood for routing
